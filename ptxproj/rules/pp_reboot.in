## SECTION=wago_specific_paramprovider

config PP_REBOOT
	bool
	select HOST_CT_BUILD
	select GOOGLETEST
	select WDX_CORE
	select WDX_WDM
	select WDX_WDM_CPP_HEADERS
	prompt "pp_reboot"
	help
	  Parameter provider that allows to reboot the device
