## SECTION=elrest_specific_applications_browser

config WEBENGINEBROWSER
	bool
	prompt "webenginebrowser qt5"
	select QT5
	select MUSTACHE
	select LIBMSGTOOL
	help
	  qt5 webengine based browser

if WEBENGINEBROWSER

config WEBENGINEBROWSER_TRUNK
	bool
#	prompt "use trunk"
	help
	  Build trunk instead of latest release.

endif
